spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: never

mail:
  host: ${MAIL_HOST:smtp.fastmail.com}
  port: ${MAIL_PORT:587}
  username: ${MAIL_USERNAME:musicloud@fastmail.com}
  password: ${MAIL_PASSWORD:h3ragrnu9j4npgml}